import { AppLayoutContent } from '@/components/custom/app-layout-content';
import { AIHomepage } from '@/components/ai-platform/ai-homepage';

export const HomeAI = () => {
  return (
    <AppLayoutContent>
      <AIHomepage />
    </AppLayoutContent>
  );
};